Lobium - canvas 2D and OffscreenCanvas readback farbling (seeds.canvas).

Split out of core/config-channel.patch so the canvas surface owns its own hooks.

The shared kernel is lobium::FarbleCanvasRgba (//components/lobium_fp/lobium_farble.cc, an added
file, not part of this patch). It nudges R/G/B by {-1,0,+1} keyed on (seed, ABSOLUTE canvas
coordinate, channel), and only for pixels that are byte-identical to none of their four orthogonal
neighbours - so a solid fill, a hard edge between two fills and a cleared canvas all read back
EXACTLY, which is what a detector's known-input probe checks, while anti-aliased and blended pixels
(where the fingerprint entropy actually lives) are perturbed.

Absolute-coordinate keying is what makes the readback paths agree: a getImageData(sx,sy,w,h)
sub-rect and a full-canvas toDataURL perturb the same on-screen pixel identically.

Hook points, all reading Current() and all no-ops when seeds.canvas is 0:
  1. modules/canvas/canvas2d/base_rendering_context_2d.cc - getImageData. Farbles the freshly
     allocated, private ImageData buffer at origin (sx, sy); the live canvas is never touched, so
     repeated reads cannot compound.
  2. core/html/canvas/html_canvas_element.cc - toDataURL and toBlob. Farbles a private
     RGBA8888/UNPREMULTIPLIED copy of the snapshot (unpremultiplied so an RGB nudge can never
     produce the impossible RGB > A of an invalid premultiplied pixel). Deliberately NOT applied in
     Snapshot(), which is also the drawImage / createImageBitmap / texImage2D source - farbling
     there would perturb ordinary rendering and double-apply with getImageData.
  3. core/offscreencanvas/offscreen_canvas.cc - convertToBlob, a separate encode path. Without it a
     worker's convertToBlob would leak the true host canvas hash while getImageData on the same
     canvas returned farbled pixels: a cross-surface contradiction.

The seed is chosen by context type: seeds.canvas for a 2D context, seeds.webgl for a WebGL one (see
fingerprint/webgl-surfaces.patch for why the WebGL readback must stay Y-flip-coherent).

diff --git a/third_party/blink/renderer/core/html/canvas/canvas_rendering_context.h b/third_party/blink/renderer/core/html/canvas/canvas_rendering_context.h
--- a/third_party/blink/renderer/core/html/canvas/canvas_rendering_context.h
+++ b/third_party/blink/renderer/core/html/canvas/canvas_rendering_context.h
@@ -125,6 +125,18 @@ class CORE_EXPORT CanvasRenderingContext
   bool IsRenderingContext2D() const {
     return canvas_rendering_type_ == CanvasRenderingAPI::k2D;
   }
+
+  // Lobium: has anything been drawn into this context that could differ between HOSTS?
+  //
+  // Declared here, in core, purely so the toDataURL/toBlob path in html_canvas_element.cc can ask
+  // the question - core cannot include the modules header where the 2D answer lives.
+  // BaseRenderingContext2D overrides it; every other context type keeps the default.
+  //
+  // Defaults to TRUE, the fail-safe direction: an unknown context keeps farbling. Only the 2D
+  // context, which tracks its own draw provenance, may answer "nothing was rasterised here, these
+  // are the exact bytes the page wrote".
+  virtual bool LobiumHostEntropyDrawn() const { return true; }
+
   bool IsImageBitmapRenderingContext() const {
     return canvas_rendering_type_ == CanvasRenderingAPI::kBitmaprenderer;
   }
diff --git a/third_party/blink/renderer/core/html/canvas/html_canvas_element.cc b/third_party/blink/renderer/core/html/canvas/html_canvas_element.cc
--- a/third_party/blink/renderer/core/html/canvas/html_canvas_element.cc
+++ b/third_party/blink/renderer/core/html/canvas/html_canvas_element.cc
@@ -45,6 +45,8 @@
 #include "build/build_config.h"
 #include "cc/layers/texture_layer.h"
 #include "cc/trees/layer_tree_host.h"
+#include "components/lobium_fp/lobium_farble.h"
+#include "components/lobium_fp/lobium_fp_config.h"
 #include "services/metrics/public/cpp/ukm_recorder.h"
 #include "services/metrics/public/cpp/ukm_source_id.h"
 #include "third_party/blink/public/common/features.h"
@@ -120,6 +122,7 @@
 #include "third_party/blink/renderer/platform/graphics/paint/paint_canvas.h"
 #include "third_party/blink/renderer/platform/graphics/skia/skia_utils.h"
 #include "third_party/blink/renderer/platform/graphics/static_bitmap_image.h"
+#include "third_party/blink/renderer/platform/graphics/unaccelerated_static_bitmap_image.h"
 #include "third_party/blink/renderer/platform/graphics/static_bitmap_image_to_video_frame_copier.h"
 #include "third_party/blink/renderer/platform/graphics/static_bitmap_image_transform.h"
 #include "third_party/blink/renderer/platform/graphics/web_graphics_context_3d_video_frame_pool.h"
@@ -128,6 +131,10 @@
 #include "third_party/blink/renderer/platform/instrumentation/use_counter.h"
 #include "third_party/blink/renderer/platform/runtime_enabled_features.h"
 #include "third_party/blink/renderer/platform/wtf/functional.h"
+#include "third_party/skia/include/core/SkBitmap.h"
+#include "third_party/skia/include/core/SkColorSpace.h"
+#include "third_party/skia/include/core/SkImage.h"
+#include "third_party/skia/include/core/SkImageInfo.h"
 #include "ui/accessibility/accessibility_features.h"
 #include "ui/base/resource/resource_scale_factor.h"
 #include "ui/gfx/geometry/rect_conversions.h"
@@ -1243,6 +1250,59 @@ const AtomicString HTMLCanvasElement::ImageSourceURL() const {
       ImageEncoderUtils::kDefaultRequestedMimeType, 0, kFrontBuffer));
 }
 
+// Lobium: return a seeded-farbled copy of a canvas READBACK snapshot (for toDataURL/toBlob), or the
+// original when `seed` is 0. `seed` is seeds.canvas for a 2D canvas or seeds.webgl for a WebGL canvas;
+// the WebGL path is Y-flip-coherent with gl.readPixels because both key on the same top-down origin.
+// Reads into a private RGBA8888/unpremultiplied bitmap (unpremul so RGB nudges can't create invalid
+// premultiplied pixels) so the live canvas is never touched; farbled at origin (0,0). Applied only at
+// the toDataURL/toBlob encode entries (NOT in Snapshot, which is also the drawImage/createImageBitmap
+// source), so ordinary rendering is untouched and farbling is never applied twice.
+static scoped_refptr<StaticBitmapImage> LobiumFarbleReadback(
+    scoped_refptr<StaticBitmapImage> image,
+    uint32_t seed) {
+  if (!seed || !image) {
+    return image;
+  }
+  const gfx::Size sz = image->Size();
+  const SkImageInfo info = SkImageInfo::Make(sz.width(), sz.height(), kRGBA_8888_SkColorType,
+                                             kUnpremul_SkAlphaType, SkColorSpace::MakeSRGB());
+  SkBitmap bm;
+  if (bm.tryAllocPixels(info) &&
+      image->PaintImageForCurrentFrame().readPixels(info, bm.getPixels(), bm.rowBytes(), 0, 0)) {
+    lobium::FarbleCanvasRgba(static_cast<uint8_t*>(bm.getPixels()), sz.width(), sz.height(),
+                             bm.rowBytes(), 0, 0, seed);
+    if (sk_sp<SkImage> farbled = SkImages::RasterFromBitmap(bm)) {
+      return UnacceleratedStaticBitmapImage::Create(std::move(farbled));
+    }
+  }
+  return image;
+}
+
+// Lobium: pick the farbling seed for a canvas readback by context type - canvas 2D vs WebGL are separate
+// per-profile surfaces (seeds.canvas / seeds.webgl); returns 0 (no-op) for other contexts or no config.
+static uint32_t LobiumReadbackSeed(const CanvasRenderingContextHost* host) {
+  const lobium::LobiumFpConfig* cfg = lobium::LobiumFpConfig::Current();
+  if (!cfg || !host) {
+    return 0u;
+  }
+  if (host->IsRenderingContext2D()) {
+    // Lobium: the same provenance gate getImageData uses. A 2D canvas built only from solid
+    // fillRects holds exactly the bytes the page wrote - identical on every host - so encoding it
+    // honestly costs no unlinkability, and farbling it would contradict getImageData, which is a
+    // tell in itself. Anything rasterised (text, paths, images, gradients, shadows, filters) sets
+    // the flag. WebGL is unconditional below: its readback is always rasterised output.
+    const CanvasRenderingContext* context = host->RenderingContext();
+    if (context && !context->LobiumHostEntropyDrawn()) {
+      return 0u;
+    }
+    return cfg->seeds.canvas;
+  }
+  if (host->IsWebGL()) {
+    return cfg->seeds.webgl;
+  }
+  return 0u;
+}
+
 scoped_refptr<StaticBitmapImage> HTMLCanvasElement::Snapshot(
     SourceDrawingBuffer source_buffer) const {
   if (Size().IsEmpty()) {
@@ -1270,6 +1330,10 @@ scoped_refptr<StaticBitmapImage> HTMLCanvasElement::Snapshot(
     image_bitmap = CreateTransparentImage();
   }
 
+  // NOTE: canvas farbling is deliberately NOT applied here. Snapshot() is the shared source-image path
+  // (GetSourceImageForCanvas -> drawImage / createImageBitmap / WebGL texImage2D), not just
+  // toDataURL/toBlob, so farbling here would perturb non-readback rendering AND double-apply with the
+  // getImageData hook. The readback farble belongs on the toDataURL/toBlob/convertToBlob encode entries.
   return image_bitmap;
 }
 
@@ -1286,6 +1350,9 @@ String HTMLCanvasElement::ToDataURLInternal(
           mime_type, ImageEncoderUtils::kEncodeReasonToDataURL);
 
   scoped_refptr<StaticBitmapImage> image_bitmap = Snapshot(source_buffer);
+  // Lobium: farble the toDataURL readback per context type - seeds.canvas for 2D, seeds.webgl for WebGL
+  // (Y-flip-coherent with gl.readPixels). Other contexts are untouched.
+  image_bitmap = LobiumFarbleReadback(std::move(image_bitmap), LobiumReadbackSeed(this));
   if (image_bitmap) {
     std::unique_ptr<ImageDataBuffer> data_buffer =
         ImageDataBuffer::Create(image_bitmap);
@@ -1391,6 +1458,8 @@ void HTMLCanvasElement::toBlob(V8BlobCallback* callback,
 
   CanvasAsyncBlobCreator* async_creator = nullptr;
   scoped_refptr<StaticBitmapImage> image_bitmap = Snapshot(kBackBuffer);
+  // Lobium: farble the toBlob readback per context type (seeds.canvas for 2D, seeds.webgl for WebGL).
+  image_bitmap = LobiumFarbleReadback(std::move(image_bitmap), LobiumReadbackSeed(this));
   if (image_bitmap) {
     auto* options = ImageEncodeOptions::Create();
     options->setType(ImageEncoderUtils::MimeTypeName(encoding_mime_type));
diff --git a/third_party/blink/renderer/core/offscreencanvas/offscreen_canvas.cc b/third_party/blink/renderer/core/offscreencanvas/offscreen_canvas.cc
--- a/third_party/blink/renderer/core/offscreencanvas/offscreen_canvas.cc
+++ b/third_party/blink/renderer/core/offscreencanvas/offscreen_canvas.cc
@@ -11,6 +11,8 @@
 #include "base/metrics/histogram_functions.h"
 #include "base/numerics/safe_conversions.h"
 #include "base/task/single_thread_task_runner.h"
+#include "components/lobium_fp/lobium_farble.h"
+#include "components/lobium_fp/lobium_fp_config.h"
 #include "third_party/blink/public/common/features.h"
 #include "third_party/blink/public/platform/platform.h"
 #include "third_party/blink/public/platform/scheduler/web_agent_group_scheduler.h"
@@ -47,6 +49,9 @@
 #include "third_party/blink/renderer/platform/graphics/image.h"
 #include "third_party/blink/renderer/platform/graphics/static_bitmap_image.h"
 #include "third_party/blink/renderer/platform/graphics/static_bitmap_image_transform.h"
+#include "third_party/skia/include/core/SkBitmap.h"
+#include "third_party/skia/include/core/SkColorSpace.h"
+#include "third_party/skia/include/core/SkImageInfo.h"
 #include "third_party/blink/renderer/platform/graphics/unaccelerated_static_bitmap_image.h"
 #include "third_party/blink/renderer/platform/heap/collection_support/heap_hash_map.h"
 #include "third_party/blink/renderer/platform/heap/thread_state.h"
@@ -473,6 +478,40 @@ ScriptPromise<Blob> OffscreenCanvas::convertToBlob(
 
   base::TimeTicks start_time = base::TimeTicks::Now();
   scoped_refptr<StaticBitmapImage> image_bitmap = context_->GetImage();
+  // Lobium: farble the OffscreenCanvas convertToBlob readback for 2D contexts. This is a SEPARATE encode
+  // path from HTMLCanvasElement::toDataURL/toBlob, so without it a worker's convertToBlob would leak the
+  // true host canvas hash while getImageData on the same canvas returns farbled pixels - a cross-surface
+  // tell. Read into a private RGBA8888/unpremultiplied bitmap, farble at origin (0,0) with seeds.canvas
+  // (identical keying to getImageData), and swap in a fresh raster image; live canvas untouched.
+  // Pick the readback seed by context type: seeds.canvas for a 2D OffscreenCanvas, seeds.webgl for a
+  // WebGL one. WebGL matters especially in workers, which fingerprint via OffscreenCanvas + WebGL +
+  // convertToBlob (there is no toDataURL in a worker); without this the worker's WebGL convertToBlob
+  // would leak the pristine host pixels while gl.readPixels on the same canvas is farbled - a
+  // cross-readback tell. The convertToBlob snapshot is top-down, so FarbleCanvasRgba at origin (0,0) is
+  // coherent with both the HTMLCanvas WebGL toDataURL path and the (flipped-row) readPixels path.
+  uint32_t readback_seed = 0;
+  if (const lobium::LobiumFpConfig* cfg = lobium::LobiumFpConfig::Current(); cfg && context_) {
+    if (context_->IsRenderingContext2D()) {
+      readback_seed = cfg->seeds.canvas;
+    } else if (context_->IsWebGL()) {
+      readback_seed = cfg->seeds.webgl;
+    }
+  }
+  if (readback_seed && image_bitmap) {
+    const gfx::Size sz = image_bitmap->Size();
+    const SkImageInfo info = SkImageInfo::Make(sz.width(), sz.height(), kRGBA_8888_SkColorType,
+                                               kUnpremul_SkAlphaType, SkColorSpace::MakeSRGB());
+    SkBitmap bm;
+    if (bm.tryAllocPixels(info) &&
+        image_bitmap->PaintImageForCurrentFrame().readPixels(info, bm.getPixels(), bm.rowBytes(), 0,
+                                                             0)) {
+      lobium::FarbleCanvasRgba(static_cast<uint8_t*>(bm.getPixels()), sz.width(), sz.height(),
+                               bm.rowBytes(), 0, 0, readback_seed);
+      if (sk_sp<SkImage> farbled = SkImages::RasterFromBitmap(bm)) {
+        image_bitmap = UnacceleratedStaticBitmapImage::Create(std::move(farbled));
+      }
+    }
+  }
   if (image_bitmap) {
     auto* resolver = MakeGarbageCollected<ScriptPromiseResolver<Blob>>(
         script_state, exception_state.GetContext());
diff --git a/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.cc b/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.cc
--- a/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.cc
+++ b/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.cc
@@ -27,6 +27,8 @@
 #include "cc/paint/paint_flags.h"
 #include "cc/paint/paint_image.h"
 #include "cc/paint/record_paint_canvas.h"
+#include "components/lobium_fp/lobium_farble.h"
+#include "components/lobium_fp/lobium_fp_config.h"
 #include "components/viz/common/resources/shared_image_format_utils.h"
 #include "third_party/abseil-cpp/absl/cleanup/cleanup.h"
 #include "third_party/blink/public/common/metrics/document_update_reason.h"
@@ -103,6 +105,8 @@
 #include "third_party/blink/renderer/platform/wtf/forward.h"
 #include "third_party/blink/renderer/platform/wtf/math_extras.h"
 #include "third_party/blink/renderer/platform/wtf/text/wtf_string.h"
+#include "third_party/skia/include/core/SkBitmap.h"
+#include "third_party/skia/include/core/SkImageInfo.h"
 #include "ui/gfx/geometry/skia_conversions.h"
 #include "ui/gfx/geometry/vector2d_f.h"
 
@@ -513,6 +517,53 @@ ImageData* BaseRenderingContext2D::getImageDataInternal(
       SkIRect bounds =
           snapshot->PaintImageForCurrentFrame().GetSkImageInfo().bounds();
       DCHECK(!bounds.intersect(SkIRect::MakeXYWH(sx, sy, sw, sh)));
+    } else if (const lobium::LobiumFpConfig* cfg = lobium::LobiumFpConfig::Current();
+               cfg && cfg->seeds.canvas && LobiumHostEntropyDrawn() &&
+               image_data_pixmap.colorType() == kRGBA_8888_SkColorType) {
+      // LobiumHostEntropyDrawn(): only perturb a canvas that had something RASTERISED into it -
+      // glyphs, paths, images, gradients, shadows, filters. A canvas built purely from solid
+      // fillRects holds exactly the bytes the page asked for, identical on every machine, so
+      // farbling it buys no unlinkability while being trivially detectable: CreepJS's getPixelMods
+      // writes 64 random opaque colours that way and calls any difference a lie. See
+      // Canvas2DRecorderContext::LobiumNoteDraw for the classification.
+      //
+      // Lobium: seeded canvas farbling. image_data_pixmap is the freshly allocated, private
+      // ImageData buffer - never the live canvas - so perturbing it in place cannot compound
+      // across reads. The colorType guard matters: getImageData({storageFormat:'float16'|
+      // 'float32'}) hands back a 8- or 16-byte-per-pixel buffer, and the RGBA8888 kernel would
+      // mangle a fraction of each row and nudge float EXPONENT bytes. Wide-gamut/HDR canvases are
+      // therefore left alone rather than corrupted.
+      //
+      // The flat-run test that keeps solid fills byte-exact has to be evaluated against the pixels
+      // AROUND the requested rectangle, not against the rectangle itself. Evaluated on the
+      // rectangle, the answer depends on what the page asked for: getImageData(x, y, 1, 1) has no
+      // neighbours of its own, so it would never be farbled - letting a page read the pristine
+      // canvas one pixel at a time AND prove tampering by comparing against a full-canvas read.
+      // So read a one-pixel apron and decide flatness from that. Clipping the apron at the canvas
+      // edge is correct, because a full-canvas read sees exactly the same clipping for that pixel.
+      gfx::Rect in_bounds = image_data_rect;
+      in_bounds.Intersect(snapshot_rect);
+      gfx::Rect apron(in_bounds.x() - 1, in_bounds.y() - 1, in_bounds.width() + 2,
+                      in_bounds.height() + 2);
+      apron.Intersect(snapshot_rect);
+      SkBitmap apron_bitmap;
+      const SkImageInfo apron_info = image_data_pixmap.info().makeWH(
+          apron.width(), apron.height());
+      if (!in_bounds.IsEmpty() && apron_bitmap.tryAllocPixels(apron_info) &&
+          snapshot->PaintImageForCurrentFrame().readPixels(
+              apron_info, apron_bitmap.getPixels(), apron_bitmap.rowBytes(), apron.x(),
+              apron.y())) {
+        // Address the first in-bounds pixel through SkPixmap rather than by hand: this file is not
+        // on the -Wunsafe-buffer-usage opt-out list, so raw pointer arithmetic here is a hard
+        // -Werror. writable_addr(x, y) does the same indexing inside Skia, with bounds assertions.
+        uint8_t* dst = static_cast<uint8_t*>(image_data_pixmap.writable_addr(
+            in_bounds.x() - image_data_rect.x(), in_bounds.y() - image_data_rect.y()));
+        lobium::FarbleCanvasRgbaSubRect(
+            dst, in_bounds.width(), in_bounds.height(), image_data_pixmap.rowBytes(),
+            static_cast<const uint8_t*>(apron_bitmap.getPixels()), apron.width(),
+            apron.height(), apron_bitmap.rowBytes(), in_bounds.x() - apron.x(),
+            in_bounds.y() - apron.y(), apron.x(), apron.y(), cfg->seeds.canvas);
+      }
     }
   }
 
diff --git a/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.h b/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.h
--- a/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.h
+++ b/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.h
@@ -75,6 +75,13 @@ enum class PredefinedColorSpace;
 class MODULES_EXPORT BaseRenderingContext2D : public CanvasRenderingContext,
                                               public Canvas2DRecorderContext {
  public:
+  // Lobium: both bases declare this name - CanvasRenderingContext as the virtual the core-side
+  // toDataURL/toBlob path calls, Canvas2DRecorderContext as the flag the draw funnel maintains.
+  // Overriding here joins them and removes the ambiguity that inheriting both would otherwise cause.
+  bool LobiumHostEntropyDrawn() const override {
+    return Canvas2DRecorderContext::LobiumHostEntropyDrawn();
+  }
+
   static constexpr unsigned kFallbackToCPUAfterReadbacks = 2;
 
   // Try to restore context 4 times in the event that the context is lost. If
diff --git a/third_party/blink/renderer/modules/canvas/canvas2d/canvas_2d_recorder_context.cc b/third_party/blink/renderer/modules/canvas/canvas2d/canvas_2d_recorder_context.cc
--- a/third_party/blink/renderer/modules/canvas/canvas2d/canvas_2d_recorder_context.cc
+++ b/third_party/blink/renderer/modules/canvas/canvas2d/canvas_2d_recorder_context.cc
@@ -831,6 +831,9 @@ void Canvas2DRecorderContext::ResetInternal() {
   state_stack_.resize(1);
   state_stack_.front() = MakeGarbageCollected<CanvasRenderingContext2DState>();
   layer_count_ = 0;
+  // Lobium: the canvas contents are gone, so the draw provenance that justified farbling is gone
+  // with them. This is the path `canvas.width = N` and ctx.reset() take.
+  lobium_host_entropy_drawn_ = false;
   SetIsTransformInvertible(true);
 
   CanvasPath::Clear();
diff --git a/third_party/blink/renderer/modules/canvas/canvas2d/canvas_2d_recorder_context.h b/third_party/blink/renderer/modules/canvas/canvas2d/canvas_2d_recorder_context.h
--- a/third_party/blink/renderer/modules/canvas/canvas2d/canvas_2d_recorder_context.h
+++ b/third_party/blink/renderer/modules/canvas/canvas2d/canvas_2d_recorder_context.h
@@ -459,6 +459,9 @@ class MODULES_EXPORT Canvas2DRecorderContext : public CanvasPath {
   unsigned max_state_stack_depth_ = 1;
   // Counts how many states have been pushed with BeginLayer.
   int layer_count_ = 0;
+  // Lobium: set by LobiumNoteDraw when something rasterised is drawn; cleared by ResetInternal,
+  // which is what `canvas.width = N` and ctx.reset() go through. Gates canvas farbling at readback.
+  bool lobium_host_entropy_drawn_ = false;
   AntiAliasingMode clip_antialiasing_ = kNotAntiAliased;
   // The paint worklet canvas operates on CSS pixels, and that's different than
   // the HTML canvas which operates on physical pixels. In other words, the
@@ -505,6 +508,57 @@ class MODULES_EXPORT Canvas2DRecorderContext : public CanvasPath {
   virtual void WillDraw(const gfx::Rect& dirty_rect,
                         CanvasPerformanceMonitor::DrawType) = 0;
 
+  // Lobium: has anything been drawn into this canvas that could differ between HOSTS?
+  //
+  // Canvas farbling has to answer one question at readback time: are these pixels a fingerprint, or
+  // are they exactly what the page asked for? The pixel buffer cannot answer it - 64 random colours
+  // written with fillRect are byte-indistinguishable from 64 pixels of rendered text. But Blink
+  // knows the PROVENANCE, and Chromium already classifies every draw for the performance monitor,
+  // so the answer is available for free one layer up.
+  //
+  // This is what CreepJS's getPixelMods exploits (creep.js L2913-2992): it fills an 8x8 canvas with
+  // random opaque colours via fillRect, reads each pixel back, and calls any difference a lie. Those
+  // 64 pixels carry ZERO cross-host entropy - an exact sRGB colour is the same on every machine - so
+  // perturbing them buys no unlinkability and is trivially detectable.
+  //
+  // Real canvas entropy is in rasterisation: glyphs, path anti-aliasing, decoded images, gradient
+  // interpolation, shadow blur, filters. Those set the flag; plain rectangles of a solid colour do
+  // not. The gate only ever makes FEWER pixels move, so it cannot regress known-input fidelity,
+  // idempotence, or read-rectangle independence.
+  bool LobiumHostEntropyDrawn() const { return lobium_host_entropy_drawn_; }
+
+  // Note a draw against the entropy flag. Deliberately NOT keyed on DrawType alone: a fillRect
+  // painted with a gradient or a pattern, or under a shadow or a filter, is rasterised and does
+  // carry host entropy even though its DrawType is kRectangle.
+  void LobiumNoteDraw(CanvasPerformanceMonitor::DrawType draw_type) {
+    if (lobium_host_entropy_drawn_) {
+      return;
+    }
+    switch (draw_type) {
+      case CanvasPerformanceMonitor::DrawType::kText:
+      case CanvasPerformanceMonitor::DrawType::kPath:
+      case CanvasPerformanceMonitor::DrawType::kImage:
+      case CanvasPerformanceMonitor::DrawType::kElement:
+        lobium_host_entropy_drawn_ = true;
+        return;
+      default:
+        break;
+    }
+    // kRectangle / kImageData / kOther: only entropy-carrying via the paint state.
+    //
+    // kOther in particular must NOT mark on its own - ResetInternal() fires it for every
+    // `canvas.width = N` assignment, and getPixelMods does exactly that before it draws. Marking
+    // there would defeat the whole gate.
+    const CanvasRenderingContext2DState& state = GetState();
+    if (state.ShouldDrawShadows() ||
+        state.HasPattern(CanvasRenderingContext2DState::kFillPaintType) ||
+        state.HasPattern(CanvasRenderingContext2DState::kStrokePaintType) ||
+        state.FillStyle().GetCanvasGradient() ||
+        state.StrokeStyle().GetCanvasGradient()) {
+      lobium_host_entropy_drawn_ = true;
+    }
+  }
+
   virtual sk_sp<PaintFilter> StateGetFilter() = 0;
 
   virtual CanvasRenderingContextHost* GetCanvasRenderingContextHost() const {
@@ -824,6 +878,7 @@ void Canvas2DRecorderContext::DrawInternal(
   const CanvasRenderingContext2DState& state = GetState();
   SkBlendMode global_composite = state.GlobalComposite();
   if (ShouldUseCompositedDraw(paint_type, image_type)) {
+    LobiumNoteDraw(draw_type);
     WillDraw(gfx::SkIRectToRect(clip_bounds), draw_type);
     CompositedDraw(draw_func, paint_canvas, paint_type, image_type);
     ResetAlphaIfNeeded(paint_canvas, global_composite);
@@ -832,6 +887,7 @@ void Canvas2DRecorderContext::DrawInternal(
     paint_canvas->clear(HasAlpha() ? SkColors::kTransparent : SkColors::kBlack);
     const cc::PaintFlags* flags =
         state.GetFlags(paint_type, kDrawForegroundOnly, image_type);
+    LobiumNoteDraw(draw_type);
     WillDraw(gfx::SkIRectToRect(clip_bounds), draw_type);
     draw_func(paint_canvas, flags);
     ResetAlphaIfNeeded(paint_canvas, global_composite, &bounds);
@@ -848,7 +904,8 @@ void Canvas2DRecorderContext::DrawInternal(
           CheckOverdraw(flags, image_type, CurrentOverdrawOp);
         }
       }
-      WillDraw(gfx::SkIRectToRect(dirty_rect), draw_type);
+      LobiumNoteDraw(draw_type);
+    WillDraw(gfx::SkIRectToRect(dirty_rect), draw_type);
       draw_func(paint_canvas, flags);
       ResetAlphaIfNeeded(paint_canvas, global_composite, &bounds);
     }
