Index: src/chrome/browser/resources/new_tab_page/app.css
===================================================================
--- src.orig/chrome/browser/resources/new_tab_page/app.css
+++ src/chrome/browser/resources/new_tab_page/app.css
@@ -198,6 +198,29 @@ cr-most-visited {
   z-index: 2;
 }
 
+/* Lobster Browser ad directly under the search box. */
+#lobiumSubBrand {
+  display: flex;
+  justify-content: center;
+  align-items: center;
+  margin: 4px auto 16px;
+  width: var(--ntp-search-box-width, min(337px, 80vw));
+}
+
+#lobiumSubBrand[hidden] {
+  display: none;
+}
+
+#lobiumSubBrand img {
+  display: block;
+  width: 100%;
+  height: auto;
+  max-width: var(--ntp-search-box-width, min(337px, 80vw));
+  object-fit: contain;
+  user-select: none;
+  -webkit-user-drag: none;
+}
+
 #modules:not([hidden]) {
   /* We use animation instead of transition to allow a fade-in out of
      display: none. */
Index: src/chrome/browser/resources/new_tab_page/app.html
===================================================================
--- src.orig/chrome/browser/resources/new_tab_page/app.html
+++ src/chrome/browser/resources/new_tab_page/app.html
@@ -99,6 +99,10 @@ import {SubmitButtonIconType} from '//re
       </ntp-lens-upload-dialog>
     ` : ''}
   </div>
+  <!-- Lobster Browser ad directly under the search box. -->
+  <div id="lobiumSubBrand" ?hidden="${!this.logoEnabled_}">
+    <img src="icons/lobster_ad.png" alt="Lobster Browser for multiple profile management" draggable="false">
+  </div>
   ${this.lazyRender_ && this.ntpNextFeaturesEnabled_
   && this.isActionChipsVisible_ && this.composeboxEnabled ? html`
     <ntp-action-chips
Index: src/chrome/browser/resources/new_tab_page/icons/BUILD.gn
===================================================================
--- src.orig/chrome/browser/resources/new_tab_page/icons/BUILD.gn
+++ src/chrome/browser/resources/new_tab_page/icons/BUILD.gn
@@ -12,6 +12,10 @@ generate_grd("build_grdp") {
   grd_prefix = "new_tab_page"
   out_grd = "$target_gen_dir/resources.grdp"
   input_files = [
+    "lobster_ad.png",
+    "lobium_master.png",
+    "lobster_wordmark.png",
+    "lobster_wordmark_horizontal.png",
     "attach_file.svg",
     "chevron.svg",
     "copy.svg",
Index: src/chrome/browser/resources/new_tab_page/logo.css
===================================================================
--- src.orig/chrome/browser/resources/new_tab_page/logo.css
+++ src/chrome/browser/resources/new_tab_page/logo.css
@@ -31,27 +31,31 @@
 }
 
 #logo {
+  background: none;
   forced-color-adjust: none;
-  height: 92px;
-  width: 272px;
+  height: auto;
+  width: auto;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  margin: 0 auto;
 }
 
-:host([use-google-logo26_]) #logo {
-  height: 82px;
-  width: 270px;
-}
-
-:host([single-colored]) #logo {
-  -webkit-mask-image: url(./icons/google_logo.svg);
-  -webkit-mask-repeat: no-repeat;
-  -webkit-mask-size: 100%;
-  background-color: var(--ntp-logo-color);
+#logoImage {
+  display: block;
+  width: auto;
+  height: auto;
+  max-width: min(440px, 74vw);
+  max-height: 260px;
+  object-fit: contain;
+  user-select: none;
+  -webkit-user-drag: none;
 }
 
+:host([single-colored]) #logo,
 :host(:not([single-colored])) #logo {
-  background-image: url(./icons/google_logo.svg);
-  background-repeat: no-repeat;
-  background-size: 100%;
+  -webkit-mask-image: none;
+  background-image: none;
 }
 
 #imageDoodle {
Index: src/chrome/browser/resources/new_tab_page/logo.html
===================================================================
--- src.orig/chrome/browser/resources/new_tab_page/logo.html
+++ src/chrome/browser/resources/new_tab_page/logo.html
@@ -1,5 +1,7 @@
 ${this.showLogo_ ? html`
-  <div id="logo"></div>
+  <div id="logo" aria-label="Lobster Browser">
+    <img id="logoImage" src="icons/lobium_master.png" alt="Lobster Browser" draggable="false">
+  </div>
 ` : ''}
 ${this.showDoodle_ ? html`
   <div id="doodle" title="${this.doodle_!.description}">
Index: src/chrome/browser/resources/new_tab_page/logo.ts
===================================================================
--- src.orig/chrome/browser/resources/new_tab_page/logo.ts
+++ src/chrome/browser/resources/new_tab_page/logo.ts
@@ -175,7 +175,10 @@ export class LogoElement extends CrLitEl
   }
 
   private computeShowDoodle_(): boolean {
-    return !!this.imageDoodle_;
+    // Lobium: NEVER show the Google logo or the animated Google doodle. Only our own brand image
+    // (icons/lobium_master.png) appears above the search box - gated by showLogo_ = loaded_ &&
+    // !showDoodle_, which stays true because the doodle is always suppressed here.
+    return false;
   }
 
   /**
Index: src/chrome/browser/ui/webui/new_tab_page/new_tab_page_handler.cc
===================================================================
--- src.orig/chrome/browser/ui/webui/new_tab_page/new_tab_page_handler.cc
+++ src/chrome/browser/ui/webui/new_tab_page/new_tab_page_handler.cc
@@ -698,17 +698,10 @@ void NewTabPageHandler::GetMostVisitedSe
 }
 
 void NewTabPageHandler::GetDoodle(GetDoodleCallback callback) {
-  bool enable_animated_logo =
-      base::FeatureList::IsEnabled(ntp_features::kNtpAnimatedDoodles) &&
-      !gfx::Animation::PrefersReducedMotion();
-  search_provider_logos::LogoCallbacks callbacks;
-  callbacks.on_cached_encoded_logo_available =
-      base::BindOnce(&NewTabPageHandler::OnLogoAvailable,
-                     weak_ptr_factory_.GetWeakPtr(), std::move(callback));
-  // This will trigger re-downloading the doodle and caching it. This means a
-  // new doodle will be returned on subsequent NTP loads.
-  logo_service_->GetLogo(std::move(callbacks), /*for_webui_ntp=*/true,
-                         enable_animated_logo);
+  // Lobium: never fetch or serve the Google doodle. The NTP shows only our own brand logo
+  // (see logo.ts computeShowDoodle_ => false). Returning null here also stops the wasted network
+  // request to Google's doodle endpoint on every NTP load.
+  std::move(callback).Run(nullptr);
 }
 
 void NewTabPageHandler::UpdatePromoData() {
