# Copy to `.env` and fill in real values locally. NEVER commit `.env` (see root .gitignore).

# Venice AI (OpenAI-compatible) — https://docs.venice.ai
# Base URL used by the client: https://api.venice.ai/api/v1
VENICE_API_KEY=replace-with-your-venice-api-key
