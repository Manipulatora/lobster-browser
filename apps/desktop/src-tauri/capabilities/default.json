{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for the main window: core Tauri APIs plus permission to execute helper/engine processes via the shell plugin.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:allow-execute"]
}
