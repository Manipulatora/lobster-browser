import { ChangeDetectionStrategy, Component } from '@angular/core';

/** SignInPage — intentionally empty. */
@Component({
  selector: 'app-sign-in-page',
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: ``,
})
export class SignInPage {}
