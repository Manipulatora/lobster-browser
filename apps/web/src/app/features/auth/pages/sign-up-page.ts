import { ChangeDetectionStrategy, Component } from '@angular/core';

/** SignUpPage — intentionally empty. */
@Component({
  selector: 'app-sign-up-page',
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: ``,
})
export class SignUpPage {}
