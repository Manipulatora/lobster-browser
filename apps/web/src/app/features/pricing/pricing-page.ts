import { ChangeDetectionStrategy, Component } from '@angular/core';

/** Pricing page — intentionally empty. */
@Component({
  selector: 'app-pricing-page',
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: ``,
})
export class PricingPage {}
