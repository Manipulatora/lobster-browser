{
  "name": "@lobster/fingerprint",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Deterministic seed -> coherent fingerprint derivation + coherence rules for Lobster profiles.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "tsc -b && node --test \"dist/**/*.test.js\"",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@lobster/shared-types": "*",
    "fingerprint-generator": "^2"
  }
}
