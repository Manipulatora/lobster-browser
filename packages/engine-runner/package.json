{
  "name": "@lobster/engine-runner",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Node/TS sidecar: launches and controls the Lobium + Chromium engines for the Rust desktop core.",
  "bin": {
    "lobster-engine-runner": "./dist/index.js"
  },
  "exports": {
    ".": {
      "types": "./dist/lib.d.ts",
      "default": "./dist/lib.js"
    }
  },
  "types": "./dist/lib.d.ts",
  "main": "./dist/lib.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "tsc -b && node --test \"dist/**/*.test.js\"",
    "start": "node dist/index.js",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@lobster/fingerprint": "*",
    "@lobster/proxy": "*",
    "@lobster/shared-types": "*",
    "patchright": "^1.61.1",
    "proxy-chain": "^3.0.0"
  }
}
