{
  "name": "@lobster/crypto",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Client-side AES-256-GCM profile-blob encryption (SEC-1 LBv1 envelope).",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "tsc -b && node --test \"dist/**/*.test.js\"",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@noble/hashes": "^2.2.0"
  }
}
